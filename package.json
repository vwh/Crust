{
  "name": "crust",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "repl": "bun run index.ts",
    "test": "bun run index.ts test.crust",
    "tests": "bun test ./tests/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
